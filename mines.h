#include <iostream>
#include <stack>

class mines
{

    // store mine value
    int mine_value = -1;
    // total no. of mines
    int totalMines;
    // mine locations in a stack or - not needed here
    std::stack<std::pair<int, int>> mineStk;

    // fillmine helper function

    // return the mines location as a array - function
};